---
description: Start Astro dev server in background mode
---

Start the Astro development server in background mode. Run from the project root:

```bash
astro dev --background
```

If a server is already running, report its PID instead of starting a new one.
